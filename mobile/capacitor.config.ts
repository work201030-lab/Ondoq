import type { CapacitorConfig } from '@capacitor/cli';
const config: CapacitorConfig = {
  appId: 'com.ondoq.clinic',
  appName: 'ONDOQ',
  webDir: 'dist',
  server: { androidScheme: 'https' }
};
export default config;
